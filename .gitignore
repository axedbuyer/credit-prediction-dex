# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
out/
dist/
build/
.next/
cache/

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# Environment — ignore every .env variant (incl. backup copies) except examples
.env*
!.env*.example

# Secrets
*token*.txt
*token*.json
*.key
*.pem

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.Zone.Identifier
*:Zone.Identifier

# Logs
*.log
npm-debug.log*

# Redis dump
dump.rdb

# Local demo stack runtime state (PIDs, logs, addresses, dev private keys)
scripts/demo/.run/
# node_modules symlink (→ backend/matching-engine) — dir-only node_modules/ misses it
scripts/demo/node_modules
