{
  "name": "funding-keeper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node --env-file=.env -r ts-node/register funding-keeper.ts",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
